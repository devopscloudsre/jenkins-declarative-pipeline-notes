pipeline {
    agent any
    stages {
        stage('Build') {
        steps {

            echo "$BRANCH_NAME"
            echo 'Pulling...' + env.BRANCH_NAME
            echo "hello world"

        }
    }
        }
    }
